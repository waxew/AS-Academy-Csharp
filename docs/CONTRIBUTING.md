# راهنمای مشارکت

- Stable IDهای منتشرشده تغییر نکنند.
- فایل‌های Course با قرارداد `AS-Academy-Core/docs/course-contract.md` سازگار باشند.
- منطق عمومی اپ در این ریپو اضافه نشود.
- مثال‌های C# باید Nullable فعال داشته باشند.
- کد آموزشی باید کامنت‌های توضیحی هدفمند داشته باشد.
- Secret، token و credential واقعی commit نشود.
- هر درس جدید حداقل یک مثال یا تمرین داشته باشد.
- تغییر محتوایی با SemVer نسخه Course هماهنگ شود.
