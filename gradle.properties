org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
android.useAndroidX=true
kotlin.code.style=official
# Temporary compatibility switch for the existing generated course-assets Copy task.
# The app keeps course content in the course package instead of duplicating it in app/src/main/assets.
android.sourceset.disallowProvider=false
